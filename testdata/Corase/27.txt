Requests by any user to manage public access blocks, list buckets, and manage S3 jobs should be allowed. Requests by any user to perform all actions on the mybucket should be allowed. Requests by any user to delete bucket configurations and object versions in mybucket should be denied.
